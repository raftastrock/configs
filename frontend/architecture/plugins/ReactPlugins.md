# Making a pluggable architecture with React

> The props passed to an injected component are essentially the component’s API.

* So you can pass allowable props to desired component:

```js
Iris.Component.Card(Props)
```